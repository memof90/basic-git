import Uikit 

class ViewController: UIViewController {

    let myName = "Guillermo"
    let color = "blue"
    let favoriteFood = "Buguger"

   override func viewDidload() {
        super.viewDidload()

        myFavoriteFood(foodName: "burger")
    }

    func myPersonalDescription() -> String {
        
       return "my name" + myName + green + favoriteFood
    }

    func myFavoriteFood(foodName: String) {
        print(foodName)
    }
}